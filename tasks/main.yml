---
- name: Downloads Hashicorp's otto
  get_url: >
    url="{{ install.download_location }}/{{ otto.version }}/otto_{{ otto.version }}_linux_{{ otto.arch }}.zip"
    dest="/tmp/"
    mode=0755
  sudo: no
  tags:
    - hashicorp
    - otto
    - install

- name: Installs Hashicorp's otto
  shell: unzip -u -d {{ install.dir }} /tmp/otto_{{ otto.version }}_linux_{{ otto.arch }}.zip ; chmod 770 {{ install.dir }}/otto ; chown root:staff {{ install.dir }}/otto
  sudo: yes
  tags:
    - hashicorp
    - otto
    - install
