---
- name: Downloads Hashicorp's packer
  get_url: >
    url="{{ install.download_location }}/{{ packer.version }}/packer_{{ packer.version }}_linux_{{ packer.arch }}.zip"
    dest="/tmp/"
    mode=0755
  sudo: no
  tags:
    - hashicorp
    - packer
    - install

- name: Installs Hashicorp's otto
  shell: unzip -u -d {{ install.dir }} /tmp/otto_{{ otto.version }}_linux_{{ otto.arch }}.zip ; chmod 770 {{ install.dir }}/otto ; chown root:staff {{ install.dir }}/otto
  sudo: yes
  tags:
    - hashicorp
    - packer
    - install
